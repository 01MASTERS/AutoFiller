---
phase: 4
plan: 2
title: "Integration Test Suite for POST /autofill Endpoint"
wave: 2
depends_on: ["04-01"]
type: expansion
files_modified:
  - backend/src/__tests__/autofill.test.ts
autonomous: true
requirements:
  - FR-5.1
  - FR-5.2
  - FR-5.3
  - FR-5.6
  - FR-5.7
---

# Plan 04-02: Integration Test Suite for POST /autofill Endpoint

## Objective

Update Vitest tests in `backend/src/__tests__/autofill.test.ts` to test real/mocked integration of `POST /autofill` endpoint with Ollama provider, Gemini provider, custom headers, and error responses.

## Artifacts this phase produces

### Modified files
- `backend/src/__tests__/autofill.test.ts` — Comprehensive integration tests for `/autofill`.

## Tasks

<task id="04-02-T1">
<title>Update autofill.test.ts with mocked LLM gateway tests</title>
<read_first>
- backend/src/__tests__/autofill.test.ts
- backend/src/routes/api.ts
</read_first>
<action>
Update `backend/src/__tests__/autofill.test.ts`:
- Mock `LLMGateway` (or mock `fetch` / `@google/generative-ai`).
- Test `POST /autofill` with default provider (Ollama) returning valid mappings.
- Test `POST /autofill` with Gemini provider passing `x-gemini-api-key` header.
- Test `POST /autofill` returning 502 when gateway throws `LLMProviderError`.
- Test `POST /autofill` returning 400 for empty or invalid field list.
</action>
<acceptance_criteria>
- `backend/src/__tests__/autofill.test.ts` covers Ollama, Gemini, header parsing, and 502 error status.
- All tests pass with Vitest.
</acceptance_criteria>
</task>

## Verification

- `npm run test` passes 100% across all workspaces.
- `npm run lint` passes with zero warnings/errors.
