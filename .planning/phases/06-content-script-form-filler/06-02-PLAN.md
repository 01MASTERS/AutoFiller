---
phase: 6
plan: 2
title: "JSDOM Unit Test Suite for Form Filler"
wave: 2
depends_on: ["06-01"]
type: expansion
files_modified:
  - extension/src/__tests__/formFiller.test.ts
autonomous: true
requirements:
  - FR-2.1
  - FR-2.2
  - FR-2.3
  - FR-2.4
---

# Plan 06-02: JSDOM Unit Test Suite for Form Filler

## Objective

Create Vitest unit tests in `extension/src/__tests__/formFiller.test.ts` using JSDOM environment to verify value setting, event dispatching (`input`, `change`, `blur`), visual highlight class, and `FillResult` reporting.

## Artifacts this phase produces

### New files
- `extension/src/__tests__/formFiller.test.ts` — JSDOM tests for `fillFormFields()`

## Tasks

<task id="06-02-T1">
<title>Create JSDOM unit tests for formFiller</title>
<read_first>
- extension/src/content/formFiller.ts
</read_first>
<action>
Create `extension/src/__tests__/formFiller.test.ts`:
- Include `// @vitest-environment jsdom` directive.
- Test `fillFormFields`:
  - Test setting text input value by name attribute.
  - Test event listeners (`input`, `change`, `blur`) are triggered on target input.
  - Test setting textarea value.
  - Test outline highlight styling applied to filled inputs.
  - Test missing target fields are recorded in `failedFields`.
  - Test `FillResult` status values (`success`, `partial`, `error`).
</action>
<acceptance_criteria>
- `extension/src/__tests__/formFiller.test.ts` passes with `npm run test -w extension`.
- Verifies value injection, event firing, and result counts.
</acceptance_criteria>
</task>

## Verification

- `npm run test` passes 100% across all workspaces.
- `npm run lint` passes with zero errors.
