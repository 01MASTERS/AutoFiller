---
phase: 9
plan: 1
title: "Mock Form Endpoint, Troubleshooting Guidance & Integration Tests"
wave: 1
depends_on: []
type: tracer
files_modified:
  - backend/src/routes/api.ts
  - extension/src/popup/popup.ts
  - backend/src/__tests__/testForm.test.ts
autonomous: true
requirements:
  - FR-6.1
  - FR-6.2
  - NFR-1
  - NFR-2
---

# Plan 09-01: Mock Form Endpoint, Troubleshooting Guidance & Integration Tests

## Objective

Add `GET /test-form` endpoint serving a Google Form HTML fixture for local QA, enhance popup error status banner with actionable troubleshooting hints (Ollama startup, backend port check), and add integration test for the test form endpoint.

## Artifacts this phase produces

### Modified files
- `backend/src/routes/api.ts` — Add `GET /test-form` route handler
- `extension/src/popup/popup.ts` — Add error message resolution & troubleshooting hints

### New files
- `backend/src/__tests__/testForm.test.ts` — Integration tests for `/test-form`

## Tasks

<task id="09-01-T1">
<title>Implement GET /test-form endpoint in backend router</title>
<read_first>
- backend/src/routes/api.ts
</read_first>
<action>
Update `backend/src/routes/api.ts`:
- Add `GET /test-form` endpoint returning `200 OK` with `Content-Type: text/html`.
- Serves realistic Google Form DOM structure containing list items (`[role="listitem"]`), headings, inputs (`entry.101`, `entry.102`, `entry.103`), and textarea (`entry.104`).
</action>
<acceptance_criteria>
- `GET /test-form` returns HTTP 200 HTML page with Google Form text fields.
</acceptance_criteria>
</task>

<task id="09-01-T2">
<title>Add actionable troubleshooting guidance to Popup UI</title>
<read_first>
- extension/src/popup/popup.ts
</read_first>
<action>
Update `extension/src/popup/popup.ts`:
- Enhance `updateStatusBannerUI`:
  - If error includes `Ollama` or `11434`, append `" (Run 'ollama run llama3.2' to start)"`.
  - If error includes `Backend` or `3456` or `Failed to fetch`, set text to `"Backend offline. Start server with 'npm run dev'"`.
</action>
<acceptance_criteria>
- Popup UI presents user-friendly troubleshooting guidance when services are offline.
</acceptance_criteria>
</task>

<task id="09-01-T3">
<title>Create unit test for test form endpoint</title>
<read_first>
- backend/src/routes/api.ts
</read_first>
<action>
Create `backend/src/__tests__/testForm.test.ts`:
- Test `GET /test-form` returns 200 OK HTML document containing `role="listitem"` and input fields.
</action>
<acceptance_criteria>
- `backend/src/__tests__/testForm.test.ts` passes with Vitest.
</acceptance_criteria>
</task>

## Verification

- `npm run test` passes across all workspace packages.
- `npm run build` succeeds cleanly.
