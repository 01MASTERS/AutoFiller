---
phase: 2
plan: 3
title: "Endpoint Tests & Verification"
wave: 2
depends_on: ["02-01", "02-02"]
type: expansion
files_modified:
  - backend/src/__tests__/profile.test.ts
  - backend/src/__tests__/autofill.test.ts
  - backend/src/__tests__/cors.test.ts
autonomous: true
requirements:
  - FR-4.1
  - FR-4.2
  - FR-4.3
  - FR-4.4
  - FR-4.5
  - NFR-2
---

# Plan 02-03: Endpoint Tests & Verification

## Objective

Create comprehensive Vitest integration tests for `GET /health`, `GET /profile`, `POST /autofill` stub, CORS origin header verification, and Zod input validation error handling.

## Artifacts this phase produces

### New files
- `backend/src/__tests__/profile.test.ts` — Tests for `GET /profile` endpoint and schema validation
- `backend/src/__tests__/autofill.test.ts` — Tests for `POST /autofill` stub endpoint and 400 validation
- `backend/src/__tests__/cors.test.ts` — Tests for CORS headers on incoming HTTP requests

### Modified files
- None (test creation only)

### New symbols
- `profile.test.ts`: `describe('GET /profile')`
- `autofill.test.ts`: `describe('POST /autofill')`
- `cors.test.ts`: `describe('CORS Headers')`

## Tasks

<task id="02-03-T1">
<title>Create integration tests for GET /profile</title>
<read_first>
- backend/src/index.ts
- backend/src/__tests__/health.test.ts
</read_first>
<action>
Create `backend/src/__tests__/profile.test.ts`:
- Test `GET /profile` returns 200 OK
- Test response body contains `name`, `email`, `phone`
- Test response content-type is `application/json`
</action>
<acceptance_criteria>
- `backend/src/__tests__/profile.test.ts` exists
- `npx vitest run backend/src/__tests__/profile.test.ts` passes
</acceptance_criteria>
</task>

<task id="02-03-T2">
<title>Create integration tests for POST /autofill stub</title>
<read_first>
- backend/src/routes/api.ts
- shared/src/index.ts
</read_first>
<action>
Create `backend/src/__tests__/autofill.test.ts`:
- Test `POST /autofill` with valid `AutofillRequest` (e.g. `{ fields: [{ id: 'field1', label: 'First Name' }] }`) returns 200 OK and `{ status: 'success', mappings: {} }`
- Test `POST /autofill` with invalid payload (e.g. `{ fields: 'invalid' }`) returns 400 Bad Request
</action>
<acceptance_criteria>
- `backend/src/__tests__/autofill.test.ts` exists
- `npx vitest run backend/src/__tests__/autofill.test.ts` passes
</acceptance_criteria>
</task>

<task id="02-03-T3">
<title>Create CORS header verification tests and run full test suite</title>
<read_first>
- backend/src/middleware/cors.ts
</read_first>
<action>
Create `backend/src/__tests__/cors.test.ts`:
- Test requests with `Origin: chrome-extension://abcdefg` receive `Access-Control-Allow-Origin` header matching request origin
- Test requests with `Origin: http://localhost:3000` receive `Access-Control-Allow-Origin` header
- Test preflight `OPTIONS /autofill` returns correct allowed methods and headers

Run `npm run test` across all workspaces.
Run `npm run lint` and `npm run format:check`.
</action>
<acceptance_criteria>
- `backend/src/__tests__/cors.test.ts` exists and passes
- `npm run test` passes with 0 failures
- `npm run lint` and `npm run format:check` pass
</acceptance_criteria>
</task>

## Verification

- `npm run test` passes across all workspaces
- `npm run lint` and `npm run format:check` pass
- All UAT criteria for Phase 2 are verified by automated tests

## must_haves

### truths
- "GET /profile test verifies 200 status and valid profile fields"
- "POST /autofill test verifies 200 OK on valid payload and 400 Bad Request on invalid payload"
- "CORS test verifies chrome-extension and localhost origins are allowed"
- "All tests in backend workspace pass cleanly"
