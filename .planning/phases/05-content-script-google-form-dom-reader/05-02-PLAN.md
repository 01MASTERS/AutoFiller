---
phase: 5
plan: 2
title: "JSDOM Unit Test Suite for Google Form DOM Reader"
wave: 2
depends_on: ["05-01"]
type: expansion
files_modified:
  - extension/src/__tests__/domReader.test.ts
autonomous: true
requirements:
  - FR-1.1
  - FR-1.2
  - FR-1.3
  - FR-1.4
---

# Plan 05-02: JSDOM Unit Test Suite for Google Form DOM Reader

## Objective

Create Vitest unit tests in `extension/src/__tests__/domReader.test.ts` using JSDOM environment to verify field extraction against Google Forms DOM fixtures.

## Artifacts this phase produces

### New files
- `extension/src/__tests__/domReader.test.ts` — JSDOM tests for `extractFormFields()`

## Tasks

<task id="05-02-T1">
<title>Create JSDOM unit tests for domReader</title>
<read_first>
- extension/src/content/domReader.ts
</read_first>
<action>
Create `extension/src/__tests__/domReader.test.ts`:
- Include `// @vitest-environment jsdom` directive.
- Test `extractFormFields`:
  - Fixture 1: Standard Google Form text input block with heading title and `entry.12345` input.
  - Fixture 2: Textarea input block with required indicator.
  - Fixture 3: Multiple form items on a page.
  - Fixture 4: Fallback label extraction from `aria-label` or `placeholder` when title is missing.
</action>
<acceptance_criteria>
- `extension/src/__tests__/domReader.test.ts` exists and passes with `npm run test -w extension`.
- Verifies extraction of field IDs, labels, required flags, and text/textarea types.
</acceptance_criteria>
</task>

## Verification

- `npm run test` passes across all workspace packages.
- `npm run lint` passes with zero errors.
