---
phase: 7
plan: 2
title: "Unit Test Suite for Background Service Worker"
wave: 2
depends_on: ["07-01"]
type: expansion
files_modified:
  - extension/src/__tests__/background.test.ts
autonomous: true
requirements:
  - FR-3.1
  - FR-3.2
  - FR-3.3
  - FR-3.4
  - FR-3.5
---

# Plan 07-02: Unit Test Suite for Background Service Worker

## Objective

Create Vitest unit tests in `extension/src/__tests__/background.test.ts` verifying background service worker message handling, full orchestration pipeline, state transitions, storage persistence, and error scenarios with mocked Chrome APIs and fetch.

## Artifacts this phase produces

### New files
- `extension/src/__tests__/background.test.ts` — Tests for `background.ts`

## Tasks

<task id="07-02-T1">
<title>Create unit tests for background service worker</title>
<read_first>
- extension/src/background/background.ts
</read_first>
<action>
Create `extension/src/__tests__/background.test.ts`:
- Mock `chrome.tabs`, `chrome.runtime`, `chrome.storage.local`, and global `fetch`.
- Test full `'TRIGGER_AUTOFILL'` flow:
  - Scans fields from active tab.
  - Posts to backend `/autofill`.
  - Fills fields in active tab.
  - Verifies state transitions (`analyzing` -> `filling` -> `done`).
- Test backend error response updates state to `'error'`.
- Test no fields found error handling.
- Test `'GET_STATUS'` action returning saved status.
</action>
<acceptance_criteria>
- `extension/src/__tests__/background.test.ts` exists and passes with `npm run test -w extension`.
- Verifies state transitions, message passing, and error handling.
</acceptance_criteria>
</task>

## Verification

- `npm run test` passes 100% across all workspaces.
- `npm run lint` passes with zero errors.
