---
phase: 8
plan: 3
title: "JSDOM Unit Test Suite for Popup UI"
wave: 3
depends_on: ["08-01", "08-02"]
type: expansion
files_modified:
  - extension/src/__tests__/popup.test.ts
autonomous: true
requirements:
  - FR-4.1
  - FR-4.2
  - FR-4.3
  - FR-4.4
  - FR-4.5
---

# Plan 08-03: JSDOM Unit Test Suite for Popup UI

## Objective

Create Vitest unit tests in `extension/src/__tests__/popup.test.ts` using JSDOM environment to verify popup HTML structure, storage settings initialization, provider visibility toggling, and background message dispatching.

## Artifacts this phase produces

### New files
- `extension/src/__tests__/popup.test.ts` — JSDOM tests for Popup UI

## Tasks

<task id="08-03-T1">
<title>Create JSDOM unit tests for popup UI</title>
<read_first>
- extension/src/popup/popup.ts
- extension/src/popup/popup.html
</read_first>
<action>
Create `extension/src/__tests__/popup.test.ts`:
- Include `// @vitest-environment jsdom` directive.
- Mock `chrome.storage.local`, `chrome.runtime.sendMessage`, and global `fetch`.
- Test DOM elements exist (`autofill-btn`, `provider-select`, `backend-status-pill`, `profile-card`).
- Test provider selector visibility toggle (shows Ollama model input when Ollama is selected, shows Gemini key input when Gemini is selected).
- Test clicking Auto-Fill button dispatches `TRIGGER_AUTOFILL` message to `chrome.runtime.sendMessage`.
</action>
<acceptance_criteria>
- `extension/src/__tests__/popup.test.ts` exists and passes with `npm run test -w extension`.
- Verifies DOM initialization, provider toggle, and message dispatching.
</acceptance_criteria>
</task>

## Verification

- `npm run test` passes 100% across all workspaces.
- `npm run lint` passes with zero errors.
