---
phase: 8
plan: 1
title: "Popup Design System, HTML Layout & CSS Styling"
wave: 1
depends_on: []
type: tracer
files_modified:
  - extension/src/popup/popup.html
  - extension/src/popup/popup.css
autonomous: true
requirements:
  - FR-4.1
  - FR-4.2
  - FR-4.3
  - FR-4.4
  - FR-4.5
---

# Plan 08-01: Popup Design System, HTML Layout & CSS Styling

## Objective

Create a sleek dark mode glassmorphism UI layout in `popup.html` and `popup.css` with Google Fonts typography, status indicator pills, profile preview card, LLM settings controls, and primary "Auto-Fill Form" action button.

## Artifacts this phase produces

### Modified files
- `extension/src/popup/popup.html` — Full dark mode glassmorphism HTML structure

### New files
- `extension/src/popup/popup.css` — Modern dark mode design system, flexbox layout, CSS custom properties, and micro-animations

## Tasks

<task id="08-01-T1">
<title>Create dark mode glassmorphism HTML structure in popup.html</title>
<read_first>
- extension/src/popup/popup.html
- .planning/phases/08-popup-ui/08-CONTEXT.md
</read_first>
<action>
Update `extension/src/popup/popup.html`:
- Import `popup.css` and Inter font.
- Header: App icon/logo, title `AutoFiller`, and `#backend-status-pill` connection badge.
- Action Card: `#autofill-btn` primary action button with icon & spinner.
- Status Card: `#status-banner` live status banner with state text and progress details.
- Profile Preview Card: `#profile-card` displaying `#profile-name` and `#profile-email`.
- Settings Card:
  - Provider selector (`#provider-select`): Ollama (Local) vs Gemini (Cloud).
  - Ollama options container (`#ollama-settings`): `#ollama-model-input`.
  - Gemini options container (`#gemini-settings`): `#gemini-key-input` (password input).
</action>
<acceptance_criteria>
- `popup.html` includes header, backend status pill, auto-fill button, status banner, profile preview card, and LLM settings controls.
</acceptance_criteria>
</task>

<task id="08-01-T2">
<title>Implement modern dark mode CSS design system in popup.css</title>
<read_first>
- extension/src/popup/popup.html
</read_first>
<action>
Create `extension/src/popup/popup.css`:
- Fixed width `340px`, dark background `#0f172a`, text color `#f8fafc`.
- CSS custom variables for dark glassmorphism cards (`rgba(30, 41, 59, 0.7)`), borders (`rgba(255, 255, 255, 0.1)`), and accent colors (`#3b82f6`, `#10b981`, `#ef4444`).
- Styled status pills (pulse dots for online/offline).
- Styled inputs, select dropdowns, and button hover/active focus states.
- Utility classes for hiding/showing elements (`.hidden`).
</action>
<acceptance_criteria>
- `popup.css` exists and compiles cleanly.
- Delivers a polished dark mode glassmorphism design.
</acceptance_criteria>
</task>

## Verification

- `npm run build -w extension` passes with zero errors.
